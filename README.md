# altarlabs.io

The studio site for [Altar Labs](https://altarlabs.io).

Hosted on GitHub Pages. Plain HTML + CSS, no build step.

## Editing

Just open `index.html` and `styles.css` in any editor. Push to `main` to deploy.
